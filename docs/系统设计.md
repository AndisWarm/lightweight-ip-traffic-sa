# 系统分析与设计

## 1. 需求分析

### 1.1 系统概述

`lightweight-ip-traffic-sa` 是一个面向 IP 多特征融合的轻量化安全态势感知系统。系统采用前后端分离架构，后端基于 Go、Gin、Gorm、MySQL、Redis、JWT 和 YAML 配置实现，前端基于 Vue 3、Vite、Vue Router、Pinia、Axios、Element Plus 与 ECharts 实现。

系统建设目标是打通“登录鉴权 -> 安全任务 -> 特征采集/评分 -> 总览展示 -> 预警与记录”的主链路，并在此基础上扩展真实数据源、离线流量解析、短时在线抓包和实时流量监控能力。当前系统已经形成 `system + security` 双域结构，其中 `system` 负责登录、用户、权限和审计能力，`security` 负责任务、总览、历史、预警、配置和流量增强能力。

当前系统主要解决以下问题：

- 传统 IP 风险判断维度单一，缺少基础画像、信誉、攻击面与流量行为的融合分析。
- 外部数据源依赖不稳定，需要支持本地优先、失败降级和缓存隔离。
- 安全分析结果需要面向任务、总览、历史和预警形成可追溯展示。
- 流量分析能力需要保持增强链路定位，不应阻断默认业务主链。

系统边界如下：

- 默认业务主链覆盖登录鉴权、安全任务、静态/动态特征采集、融合评分、总览展示、历史记录和预警中心。
- 流量增强链路覆盖离线 pcap/pcapng 解析、短时在线抓包、流量特征落库和任务详情承接。
- 实时流量监控为独立网卡监听能力，写入预警中心，不进入默认任务历史三表。
- 外部数据源采用可配置、可降级策略，不作为系统启动和任务创建的硬依赖。

### 1.2 功能需求

| 功能模块 | 主要功能 | 参与角色 |
| --- | --- | --- |
| 登录鉴权 | 用户登录、登出、当前用户信息获取、JWT 鉴权、路由权限控制 | 超级管理员、管理员、普通用户 |
| 用户管理 | 用户列表、新增用户、状态修改、密码重置 | 超级管理员 |
| 安全任务 | 创建任务、任务列表、任务详情、任务删除 | 超级管理员、管理员、普通用户 |
| IP 多特征采集 | GeoLite2、RDAP、本地黑名单、AbuseIPDB、端口探测、Nmap 原型、流量特征 | 超级管理员、管理员 |
| 风险评分 | 基础画像、信誉、攻击面、行为特征、规则调整和风险等级计算 | 超级管理员、管理员、普通用户 |
| 态势总览 | 汇总指标、趋势图、风险分布、地理风险聚合 | 超级管理员、管理员、普通用户 |
| 历史记录 | 任务记录与预警记录聚合查询 | 超级管理员、管理员、普通用户 |
| 预警中心 | 预警列表、预警详情、任务预警、实时流量监控预警 | 超级管理员、管理员、普通用户 |
| 安全配置 | 评分阈值、数据源开关、流量采集配置、SMTP 配置、网卡枚举 | 超级管理员、管理员 |
| 实时流量监控 | 创建监听会话、查询会话、停止会话、观察面板 | 超级管理员、管理员、普通用户 |
| 审计日志 | 登录、任务、配置、监控和预警发送行为记录 | 超级管理员 |

### 1.3 用例图

```mermaid
flowchart BT
    Admin[超级管理员]
    Manager[管理员]
    User[普通用户]

    subgraph System[轻量化 IP 流量态势感知系统]
        CommonEntry[通用功能]
        ManagerEntry[管理功能]
        AdminEntry[超级管理员功能]

        UC1((登录系统))
        UC2((查看态势总览))
        UC4((查看任务详情))
        UC5((查看历史记录))
        UC6((查看预警中心))
        UC9((启动实时流量监控))

        UC3((创建安全任务))
        UC7((维护安全配置))
        UC11((删除任务))

        UC8((管理用户))
        UC10((查看审计日志))
    end

    User --> CommonEntry
    Manager --> CommonEntry
    Manager --> ManagerEntry
    Admin --> CommonEntry
    Admin --> ManagerEntry
    Admin --> AdminEntry

    CommonEntry --> UC1
    CommonEntry --> UC2
    CommonEntry --> UC4
    CommonEntry --> UC5
    CommonEntry --> UC6
    CommonEntry --> UC9

    ManagerEntry --> UC3
    ManagerEntry --> UC7
    ManagerEntry --> UC11

    AdminEntry --> UC8
    AdminEntry --> UC10
```

### 1.4 核心用例描述

| 用例 | 触发者 | 前置条件 | 主流程 | 异常流程 | 后置结果 |
| --- | --- | --- | --- | --- | --- |
| 登录系统 | 超级管理员、管理员、普通用户 | 用户账号已启用 | 输入账号密码，后端校验密码，签发 JWT，前端保存登录态 | 密码错误、账号停用或服务异常时返回失败提示 | 用户进入授权页面 |
| 创建安全任务 | 超级管理员、管理员 | 已登录且具备任务创建权限 | 输入目标 IP，创建任务记录，执行采集、评分、预警判断，清理总览缓存 | 目标 IP 非法、采集源失败或评分异常时进入失败/降级路径 | 形成任务详情、评分结果和可能的预警 |
| 查看任务详情 | 超级管理员、管理员、普通用户 | 已登录且任务存在 | 查询任务、基础画像、特征、评分、预警和流量证据 | 任务不存在时返回错误；部分增强数据缺失时页面降级展示 | 用户获得完整证据链 |
| 维护安全配置 | 超级管理员、管理员 | 已登录且具备配置权限 | 读取配置，修改阈值、数据源、流量和邮件参数，写库并刷新缓存 | 参数非法或保存失败时返回错误 | 新配置对后续任务生效 |
| 启动实时流量监控 | 超级管理员、管理员、普通用户 | 已登录，主机具备抓包环境 | 选择网卡，创建监控会话，系统按窗口采样并判断风险 | 网卡不可用或抓包失败时会话失败 | 会话状态可查询，高风险事件写入预警中心 |

## 2. 系统静态模型

### 2.1 核心类图

```mermaid
classDiagram
    class User {
        +uint id
        +string username
        +string displayName
        +string roleCode
        +bool enable
        +Login()
        +ResetPassword()
    }

    class IpTask {
        +uint taskId
        +string taskNo
        +string targetIp
        +string taskStatus
        +int scoreValue
        +string riskLevel
        +Create()
        +Delete()
    }

    class IpBaseInfo {
        +string country
        +string region
        +string city
        +string isp
        +string whoisOrg
        +UpdateFromSource()
    }

    class FeatureSnapshot {
        +int reputationScore
        +int openPortCount
        +int highRiskPortCount
        +bool geoRiskFlag
        +string featureDigest
        +BuildDigest()
    }

    class RiskScore {
        +int scoreValue
        +string riskLevel
        +string scoreReason
        +bool isAlertTriggered
        +Calculate()
    }

    class AlertRecord {
        +uint alertId
        +string alertLevel
        +string alertTitle
        +string sourceType
        +string sendStatus
        +MarkSent()
    }

    class SecurityConfig {
        +int alertThreshold
        +bool mailEnabled
        +bool flowEnabled
        +string collectorProfile
        +Validate()
    }

    class FlowCollection {
        +uint collectionId
        +uint taskId
        +string sourceType
        +string status
        +Start()
        +Finish()
    }

    class FlowFeatureSnapshot {
        +string dnsTopQuestions
        +string httpHostHints
        +string tlsHandshakeHints
        +string applicationSignals
        +Summarize()
    }

    class AuditLog {
        +uint id
        +string action
        +string operator
        +string result
        +Record()
    }

    class AuthService {
        +Login()
        +GetCurrentUser()
        +Logout()
    }

    class TaskService {
        +CreateTask()
        +ListTasks()
        +GetTaskDetail()
        +DeleteTask()
    }

    class CollectorPipeline {
        +CollectBaseInfo()
        +CollectReputation()
        +CollectAttackSurface()
        +CollectFlowFeatures()
    }

    class ScoreService {
        +CalculateRiskScore()
        +BuildScoreReason()
    }

    class AlertService {
        +CreateAlertIfNeeded()
        +ListAlerts()
        +GetAlertDetail()
    }

    class DashboardService {
        +GetSummary()
        +GetGeoRisk()
    }

    class FlowMonitorService {
        +StartSession()
        +GetSession()
        +StopSession()
    }

    class LoginPage
    class TaskPage
    class TaskDetailPage
    class OverviewPage
    class HistoryPage
    class AlertPage
    class ConfigPage
    class FlowMonitorPage

    User "1" --> "many" IpTask : creates
    IpTask "1" --> "1" IpBaseInfo : owns
    IpTask "1" --> "1" FeatureSnapshot : owns
    IpTask "1" --> "1" RiskScore : owns
    IpTask "1" --> "0..many" AlertRecord : triggers
    IpTask "1" --> "0..many" FlowCollection : contains
    FlowCollection "1" --> "1" FlowFeatureSnapshot : produces
    SecurityConfig --> CollectorPipeline : controls
    AuthService --> User : authenticates
    TaskService --> IpTask : manages
    TaskService --> CollectorPipeline : orchestrates
    CollectorPipeline --> IpBaseInfo : fills
    CollectorPipeline --> FeatureSnapshot : fills
    ScoreService --> RiskScore : calculates
    AlertService --> AlertRecord : manages
    DashboardService --> IpTask : aggregates
    FlowMonitorService --> AlertRecord : writes
    LoginPage --> AuthService : calls
    TaskPage --> TaskService : calls
    TaskDetailPage --> TaskService : calls
    OverviewPage --> DashboardService : calls
    HistoryPage --> TaskService : queries
    AlertPage --> AlertService : calls
    ConfigPage --> SecurityConfig : edits
    FlowMonitorPage --> FlowMonitorService : calls
```

### 2.2 分层结构图

```mermaid
flowchart TB
    subgraph Web[web 前端]
        Router[Vue Router]
        Pinia[Pinia 登录态]
        Views[security/system 页面]
        ApiClient[Axios API 封装]
    end

    subgraph Server[server 后端]
        GinRouter[router 路由层]
        ApiLayer[api/v1 控制层]
        ServiceLayer[service 业务编排层]
        RepositoryLayer[repository 数据访问层]
        ModelLayer[model 请求响应与实体模型]
        Middleware[JWT 与角色中间件]
    end

    subgraph Storage[存储与缓存]
        MySQL[(MySQL)]
        Redis[(Redis)]
        LocalData[本地数据文件和 GeoLite2]
    end

    subgraph External[外部或增强数据源]
        RDAP[RDAP]
        AbuseIPDB[AbuseIPDB]
        Nmap[Nmap 原型]
        Npcap[Npcap + gopacket]
    end

    Views --> Router
    Views --> ApiClient
    ApiClient --> GinRouter
    Router --> Pinia
    GinRouter --> Middleware
    GinRouter --> ApiLayer
    ApiLayer --> ServiceLayer
    ServiceLayer --> RepositoryLayer
    ServiceLayer --> Redis
    ServiceLayer --> LocalData
    ServiceLayer --> RDAP
    ServiceLayer --> AbuseIPDB
    ServiceLayer --> Nmap
    ServiceLayer --> Npcap
    RepositoryLayer --> ModelLayer
    RepositoryLayer --> MySQL
```

### 2.3 包图

```mermaid
flowchart LR
    SystemDomain[system 域]
    SecurityDomain[security 域]

    subgraph SystemPkg[系统包]
        SystemRouter[router/system]
        SystemApi[api/v1/system]
        SystemService[service/system]
        SystemModel[model/system]
    end

    subgraph SecurityPkg[安全包]
        SecurityRouter[router/security]
        SecurityApi[api/v1/security]
        SecurityService[service/security]
        SecurityRepo[repository/security]
        SecurityModel[model/security]
    end

    subgraph FrontendPkg[前端包]
        WebApi[web/src/api]
        WebRouter[web/src/router]
        WebSecurity[web/src/view/security]
        WebSystem[web/src/view/system]
    end

    SystemDomain --> SystemRouter --> SystemApi --> SystemService --> SystemModel
    SecurityDomain --> SecurityRouter --> SecurityApi --> SecurityService --> SecurityRepo --> SecurityModel
    WebRouter --> WebSecurity
    WebRouter --> WebSystem
    WebSecurity --> WebApi
    WebSystem --> WebApi
    WebApi --> SystemApi
    WebApi --> SecurityApi
```

## 3. 系统动态模型

### 3.1 登录鉴权顺序图

```mermaid
sequenceDiagram
    actor User as 用户
    participant LoginPage as 登录页
    participant Axios as Axios 请求封装
    participant AuthApi as 认证控制器
    participant AuthService as 认证服务
    participant UserRepo as 用户仓储
    participant DB as MySQL
    participant Pinia as Pinia 登录态

    User->>LoginPage: 输入用户名和密码
    LoginPage->>Axios: POST /api/v1/system/login
    Axios->>AuthApi: 提交登录请求
    AuthApi->>AuthService: 校验登录参数
    AuthService->>UserRepo: 查询用户
    UserRepo->>DB: 按用户名读取用户
    DB-->>UserRepo: 返回用户记录
    UserRepo-->>AuthService: 返回用户对象
    AuthService->>AuthService: 校验密码和启用状态
    AuthService-->>AuthApi: 返回 token 和用户信息
    AuthApi-->>Axios: 统一响应
    Axios-->>LoginPage: 返回登录结果
    LoginPage->>Pinia: 保存 token 和用户信息
    LoginPage-->>User: 跳转到态势总览
```

### 3.2 安全任务创建与多源采集评分顺序图

```mermaid
sequenceDiagram
    actor Operator as 超级管理员或管理员
    participant TaskPage as 任务页
    participant TaskApi as 任务控制器
    participant TaskService as 任务服务
    participant Pipeline as 采集编排器
    participant Source as 数据源提供器
    participant Score as 评分服务
    participant Alert as 预警服务
    participant Repo as 安全仓储
    participant DB as MySQL
    participant Cache as Redis

    Operator->>TaskPage: 输入目标 IP 并提交
    TaskPage->>TaskApi: POST /api/v1/tasks
    TaskApi->>TaskService: CreateTask(targetIp)
    TaskService->>Repo: 创建任务记录
    Repo->>DB: 写入任务
    TaskService->>Pipeline: 执行多源采集
    Pipeline->>Source: GeoLite2 / RDAP / 黑名单 / AbuseIPDB / 端口探测
    Source-->>Pipeline: 返回基础画像和特征结果
    Pipeline->>Repo: 保存画像和特征快照
    Repo->>DB: 写入特征数据
    TaskService->>Score: 计算融合风险评分
    Score-->>TaskService: 返回评分和风险等级
    TaskService->>Repo: 保存评分结果
    TaskService->>Alert: 判断是否生成预警
    Alert->>Repo: 写入预警记录
    TaskService->>Cache: 清理总览缓存
    TaskService-->>TaskApi: 返回任务创建结果
    TaskApi-->>TaskPage: 返回 taskId、评分和预警状态
```

### 3.3 实时流量监控顺序图

```mermaid
sequenceDiagram
    actor User as 用户
    participant MonitorPage as 实时流量监控页
    participant FlowApi as 流量监控控制器
    participant FlowService as 流量监控服务
    participant Capture as Npcap + gopacket
    participant AlertService as 预警服务
    participant Repo as 安全仓储
    participant DB as MySQL

    User->>MonitorPage: 选择网卡并启动监听
    MonitorPage->>FlowApi: POST /api/v1/flow-monitor/sessions
    FlowApi->>FlowService: StartSession(interfaceName)
    FlowService->>Repo: 创建监控会话
    Repo->>DB: 保存会话状态
    FlowService->>Capture: 启动 5 秒窗口循环采样
    Capture-->>FlowService: 返回协议与行为特征
    FlowService->>FlowService: 阈值判断和风险聚合
    alt 命中预警条件
        FlowService->>AlertService: 创建 FLOW_MONITOR 预警
        AlertService->>Repo: 写入预警中心
        Repo->>DB: 保存预警记录
    end
    User->>MonitorPage: 停止监听
    MonitorPage->>FlowApi: POST /api/v1/flow-monitor/sessions/:id/stop
    FlowApi->>FlowService: StopSession(sessionId)
    FlowService->>Capture: 停止抓包
    FlowService->>Repo: 更新会话状态
```

### 3.4 主业务活动图

```mermaid
flowchart TD
    Start(["开始"])
    Login["用户登录"]
    Auth{"鉴权是否通过"}
    Overview["进入态势总览"]
    CreateTask["创建安全任务"]
    ValidateIp{"目标 IP 是否合法"}
    CollectStep["执行基础画像、信誉、攻击面、流量增强采集"]
    Degrade{"部分数据源是否失败"}
    Score["融合评分并生成说明"]
    AlertCheck{"是否达到预警阈值"}
    WriteAlert["写入预警记录"]
    WriteHistory["写入任务和历史记录"]
    Display["展示任务详情、总览趋势和证据链"]
    Finish(["结束"])
    Fail["返回错误提示"]
    Partial["记录降级信息并继续主链路"]

    Start --> Login --> Auth
    Auth -->|否| Fail
    Fail --> Finish
    Auth -->|是| Overview
    Overview --> CreateTask
    CreateTask --> ValidateIp
    ValidateIp -->|否| Fail
    ValidateIp -->|是| CollectStep
    CollectStep --> Degrade
    Degrade -->|是| Partial
    Partial --> Score
    Degrade -->|否| Score
    Score --> AlertCheck
    AlertCheck -->|是| WriteAlert
    WriteAlert --> WriteHistory
    AlertCheck -->|否| WriteHistory
    WriteHistory --> Display --> Finish
```

### 3.5 安全任务状态图

```mermaid
stateDiagram-v2
    [*] --> PENDING: 创建任务
    PENDING --> RUNNING: 开始采集
    RUNNING --> SUCCESS: 采集和评分完成
    RUNNING --> FAILED: 参数、采集或评分异常
    SUCCESS --> DELETED: 删除任务
    FAILED --> DELETED: 删除任务
    DELETED --> [*]
```

### 3.6 实时流量监控会话状态图

```mermaid
stateDiagram-v2
    [*] --> CREATED: 创建会话
    CREATED --> RUNNING: 启动网卡监听
    RUNNING --> STOPPING: 用户请求停止
    STOPPING --> STOPPED: 停止完成
    CREATED --> FAILED: 网卡不可用
    RUNNING --> FAILED: 抓包或分析异常
    STOPPED --> [*]
    FAILED --> [*]
```

## 4. 系统部署

### 4.1 组件图

```mermaid
flowchart TB
    Browser[浏览器]
    Frontend[Vue 前端应用]
    ApiServer[Go API 服务]
    Auth[认证与权限组件]
    SecurityTask[安全任务组件]
    Collector[采集器组件]
    Scoring[融合评分组件]
    Alert[预警组件]
    Dashboard[总览聚合组件]
    FlowMonitor[实时流量监控组件]
    MySQL[(MySQL)]
    Redis[(Redis)]
    GeoLite2[GeoLite2 本地库]
    LocalList[本地黑名单和 CIDR]
    RDAP[RDAP 备用端点]
    AbuseIPDB[AbuseIPDB]
    Nmap[Nmap 原型能力]
    Npcap[Npcap SDK]
    Gopacket[gopacket]

    Browser --> Frontend
    Frontend --> ApiServer
    ApiServer --> Auth
    ApiServer --> SecurityTask
    ApiServer --> Dashboard
    ApiServer --> FlowMonitor
    SecurityTask --> Collector
    SecurityTask --> Scoring
    Scoring --> Alert
    Dashboard --> Redis
    Auth --> MySQL
    SecurityTask --> MySQL
    Collector --> MySQL
    Alert --> MySQL
    Collector --> GeoLite2
    Collector --> LocalList
    Collector --> RDAP
    Collector --> AbuseIPDB
    Collector --> Nmap
    FlowMonitor --> Npcap
    FlowMonitor --> Gopacket
    FlowMonitor --> Alert
```

### 4.2 部署图

```mermaid
flowchart LR
    subgraph ClientNode[客户端节点]
        Browser[浏览器]
    end

    subgraph WebNode[前端部署节点]
        WebStatic[Vue 静态资源服务]
    end

    subgraph AppNode[后端应用节点]
        GoApi[Go API 服务]
        Config[config.yaml]
        GeoData[GeoLite2 mmdb 文件]
        Blacklist[本地黑名单文件]
        CaptureRuntime[Npcap + gopacket 运行环境]
    end

    subgraph DataNode[数据节点]
        MySQL[(MySQL 数据库)]
        Redis[(Redis 缓存)]
    end

    subgraph ExternalNode[外部增强服务]
        RDAP[RDAP 服务]
        Abuse[AbuseIPDB 服务]
        NmapExec[Nmap 执行环境]
    end

    Browser --> WebStatic
    WebStatic -->|/api/v1| GoApi
    GoApi --> Config
    GoApi --> MySQL
    GoApi --> Redis
    GoApi --> GeoData
    GoApi --> Blacklist
    GoApi --> CaptureRuntime
    GoApi --> RDAP
    GoApi --> Abuse
    GoApi --> NmapExec
```

### 4.3 部署约束

- 后端接口统一挂载在 `/api/v1` 前缀下，除 `POST /api/v1/system/login` 外，其余业务接口默认需要 JWT。
- MySQL 是任务、评分、预警、配置、流量和审计数据的主存储。
- Redis 用于总览和配置等短 TTL 缓存，Redis 不可用时系统应降级为无缓存模式。
- GeoLite2、本地黑名单和 CIDR 文件属于本地优先数据源，缺失时不得阻断任务主链路。
- RDAP、AbuseIPDB、Nmap 属于增强数据源或原型能力，必须支持超时、失败降级和配置开关。
- Npcap + gopacket 主要服务于短时在线抓包、离线流量解析和实时流量监控，不作为所有安全任务的默认前置条件。

## 5. 设计结论

本系统更适合采用“面向对象分析方法为主、结构化分析方法为辅”的设计表达方式。用户、任务、画像、特征、评分、预警、配置、流量采集和审计日志等对象具有明确属性、行为和关联关系，适合通过类图、组件图、包图和部署图描述；登录鉴权、安全任务创建、采集评分、预警生成和实时流量监控等流程具有清晰的时序和状态变化，适合通过顺序图、活动图和状态图描述。

该设计方式既能体现当前代码中的前后端分层、服务编排和仓储模型，也能满足系统设计文档对需求分析、静态模型、动态模型和部署模型的表达要求。
